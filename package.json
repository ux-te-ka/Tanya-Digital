{
  "name": "tanya-digital",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:tokens": "style-dictionary build --config config.js && style-dictionary build --config config.dark.js"
  },
  "devDependencies": {
    "style-dictionary": "^5.5.2",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
